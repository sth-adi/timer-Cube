# Optional — account sync (Google sign-in + cloud backup of solves) is
# disabled without these, and the app works fully offline regardless.
# Get these from your Supabase project's Settings > API page.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
